<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="BookInventoryApp.Home"
             Title="Home">
    <Grid>
        <Image
           Aspect="AspectFill"
           Opacity="0.3"
           IsEnabled="False">
            <Image.Source>
                <OnPlatform x:TypeArguments="ImageSource">
                    <On Platform="Android" Value="background_image_library.jpg"/>
                    <On Platform="WinUI" Value="background_image_library.jpg" />
                </OnPlatform>
            </Image.Source>
        </Image>
        <VerticalStackLayout Padding="20" Opacity="1">
            <Label Text="Lilith's Library" 
                   FontSize="Header" 
                   HorizontalOptions="Center"
                   FontAttributes="Bold"
                   FontFamily="VictorMono-Bold" 
                   TextColor="White"
                   Margin="50" Padding="20" 
                   Background="Black" Opacity="1"></Label>
            
            <SearchBar x:Name="SearchBar"
                   Placeholder="Search items..."
                   TextChanged="OnSearchTextChanged" 
                       TextColor="Black" 
                       PlaceholderColor="Black"
                       BackgroundColor="White" Opacity="0.5" 
                       MinimumHeightRequest="50" Margin="60, 60, 60, 60"/>

            <CollectionView x:Name="ResultsCollectionView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" Margin="5" BackgroundColor="MediumPurple" Opacity="01">
                            <Label Text="{Binding Title}" TextColor="White"/>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </Grid>
</ContentPage>